import { DistrictCityFilterPipe } from './district-city-filter.pipe';

describe('DistrictCityFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new DistrictCityFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
